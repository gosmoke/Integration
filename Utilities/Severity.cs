namespace Integration
{
    public enum Severity
    {
        Info = 0,
        Warning = 1,
        Error = 2,
        Invalid = 3,
        ApiValidationDisplayMessage = 5     // this need sto be mapped to Shipment messages, so leave this as a 5
    }
}
